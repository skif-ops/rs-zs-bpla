# cryptography ships win_arm64 wheels only up to 46.0.3 (checked on PyPI 2026-09-21);
# newer releases would be built from source on Windows ARM64, which needs Rust + OpenSSL.
cryptography>=46.0.0,<47; platform_machine != "ARM64"
cryptography>=46.0.0,<=46.0.3; platform_machine == "ARM64"
pytest>=8.2.0
