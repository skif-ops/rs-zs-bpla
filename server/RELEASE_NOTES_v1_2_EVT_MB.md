# Мухоед 1.2.0 EVT-MB

- Protocol 1.4 decoder and `/api/v1/health` protocol report are synchronized.
- Windows 11 Docker Desktop and Ubuntu 24.04 Docker Engine compose paths are included.
- Four-station simulation, compact-CBOR compatibility, localization modes and
  time-quality rules remain part of the test suite.
- This source release does not claim a container/runtime PASS until images are
  built and the supplied health and station simulation checks pass on the target host.
